const createResult = require("../results/createResult");
const updateResult = require("../results/updateResult");
const deleteResult = require("../results/deleteResult");

module.exports = {
	createResult,
	updateResult,
	deleteResult,
};
